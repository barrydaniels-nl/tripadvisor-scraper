# S3 Configuration
S3_BUCKET=restaurant-scraper-data

# API Configuration
API_ENDPOINT=https://viberoam.ai/api/restaurants/random/?country=NL&never_scraped=1

# Optional: Update status endpoint
# UPDATE_API_ENDPOINT=https://your-api.com/update-scraping-status

# Scraping Configuration
MAX_RETRIES=3
TIMEOUT=30

# AWS Configuration (for local testing)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1